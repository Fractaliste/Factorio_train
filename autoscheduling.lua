local stations = {}

local isManagedStation

Autoscheduling = {
    onRename = function(event)
        debug(event)
        -- Synchroniser la liste des stations qu'on gère
    end,
    onBuilt = function(event)
    end
}

isManagedStation = function()
end
